# Test dependencies. These are intentionally excluded from the production image.
pytest==9.1.1
pytest-asyncio==1.4.0
hypothesis==6.165.10
# Starlette TestClient uses this successor package instead of deprecated httpx.
httpx2==2.13.1
